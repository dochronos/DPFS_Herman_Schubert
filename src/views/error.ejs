<!DOCTYPE html>
<html lang="es">

<%- include('./partials/head.ejs', { title: 'Error', stylesheets: [] }) %>

<body>
  <%- include('./partials/header.ejs') %>

  <main class="container text-center my-5">
    <div class="p-5 shadow rounded-4" style="background-color: #f8f4ef;">
      <h1 class="mb-4 text-dark" style="font-family: 'Garamond', serif;">
        Ups... algo no salió como esperábamos
      </h1>

      <h2 class="text-muted fw-semibold mb-3" style="font-family: 'Montserrat', sans-serif;">
        <%= message %>
      </h2>

      <% if (typeof error !== 'undefined' && error?.status) { %>
        <p class="text-secondary">Código de estado: <strong><%= error.status %></strong></p>
      <% } %>

      <% if (typeof error !== 'undefined' && error?.stack) { %>
        <div class="text-start mt-4 p-3 bg-white border rounded shadow-sm overflow-auto">
          <pre class="small text-secondary"><%= error.stack %></pre>
        </div>
      <% } %>

      <a href="/" class="btn mt-4 px-4 py-2 text-white fw-semibold"
         style="background-color: #6c5ce7; border: none; font-family: 'Montserrat', sans-serif; border-radius: 12px;">
        Volver al inicio
      </a>
    </div>
  </main>

  <%- include('./partials/footer.ejs') %>
</body>

</html>
