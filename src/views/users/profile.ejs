<!DOCTYPE html>
<html lang="es">

<%- include('../partials/head.ejs', { title: 'Perfil de Usuario', stylesheets: [] }) %>

<body>
  <%- include('../partials/header.ejs') %>

  <main class="my-5">
    <div class="container">
      <section class="card mx-auto bg-light shadow border-0 rounded-4" style="max-width: 640px;">
        <div class="row g-0">
          
          <!-- Imagen de perfil -->
          <figure class="col-md-4 d-flex align-items-center justify-content-center bg-white rounded-start p-3 border-end">
            <img
              src="/images/users/<%= user.profileImage || 'user-undefined.png' %>"
              alt="Foto de perfil de <%= user.firstName %> <%= user.lastName %>"
              class="img-fluid rounded-circle border shadow"
              style="width: 100px; height: 100px; object-fit: cover;">
          </figure>

          <!-- Datos del usuario -->
          <div class="col-md-8 p-4">
            <h3 class="fw-bold mb-3"><%= user.firstName %> <%= user.lastName %></h3>

            <p class="mb-2">
              <strong>Correo electrónico:</strong><br>
              <span class="text-muted"><%= user.email %></span>
            </p>

            <p class="mb-4">
              <strong>Categoría:</strong><br>
              <span class="text-muted"><%= user.category === 'admin' ? 'Administrador' : 'Usuario' %></span>
            </p>

            <div class="d-flex flex-wrap gap-2">
              <a href="/users/edit" class="btn btn-primary">Editar usuario</a>
              <a href="/users/logout" class="btn btn-outline-secondary">Cerrar sesión</a>
              <form action="/users/<%= user.id %>?_method=DELETE" method="POST" class="d-inline"
                    onsubmit="return confirm('¿Estás seguro de que querés eliminar este usuario?');">
                <button type="submit" class="btn btn-danger">Eliminar usuario</button>
              </form>
            </div>
          </div>
        </div>
      </section>
    </div>
  </main>

  <%- include('../partials/footer.ejs') %>
</body>

</html>
